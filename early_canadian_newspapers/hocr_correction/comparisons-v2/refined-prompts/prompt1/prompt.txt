# YOUR ROLE: Historical Archivist (Quebec Gazette - Late 18th century)

# TASK: Correct OCR errors in the provided JSON dictionary (ID -> Text) using the provided image as reference.

# INSTRUCTIONS:
1. Trust text over image: 
    - If the input text is already valid correctly spelled word (e.g., 'situation'), Do NOT change it. 
    - Always trust the text over image. NEVER change a real word into nonsense (e.g. 'situation' -> 'ſituation').
2. Preserve Numbers: 
    - Assume all digits/numbers are correct. NEVER change them.
3. Standardize S: 
    - Convert all historical  Long S (ſ) to modern 's'. 
    - NEVER output 'ſ'.
    - NEVER change 's' to 'ſ'.
    - NEVER change 's' to 'f'.
    - NEVER change 'ſ' to 'f'.
4. Do not add diacritics: Do NOT add new accents/diacritics. Maintain the original text's lack of accents.

# KNOWN ERRORS:
1. Target Fixes:
    - Prioritize 'f' -> 's' (e.g., 'purofe' -> 'purpose') and lookalikes ('u'/'o', 'l'/'i'/'t').
2. Perserve Hyphen:
    - If a word segment ends with a hyphen ('-'), you MUST preserve the hyphen.
3. Validation: 
    - ONLY CHANGE text if the result is a recognizable historical/modern word.
4. No Modernization: 
    - KEEP archaic spellings (e.g., 'publick', 'colour').

# GENERAL CONSTRAINTS:
1. Word-by-Word Review: 
    - Process every key INDIVIDUALLY against the logic before final output.
2. Grammar/Punctuation:
    - Do NOT correct grammar, punctuation, or sentence structure.
3. Internal Knowledge Only: 
    - ONLY use your internal linguistic knowledge of English and French to validate words.
    - Do NOT use external internet search. 

# OUTPUT FORMAT:
- Return ONLY raw JSON. No Markdown. No preamble.
- Maintain EXACT original given keys.

# INPUT JSON:
{json.dumps(payload)}